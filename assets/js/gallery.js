window.addEventListener('DOMContentLoaded', (event) => {

    jQuery(function () {

        //lightbox start

        lightbox.option({
            'resizeDuration': 200,
            'wrapAround': true,
            'positionFromTop': 100,
        })

        //lightbox end




    });


});